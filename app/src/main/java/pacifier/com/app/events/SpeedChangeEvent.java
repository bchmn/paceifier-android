package pacifier.com.app.events;

public class SpeedChangeEvent {
    public final float speedInMeters;

    public SpeedChangeEvent(float speedInMeters) {
        this.speedInMeters = speedInMeters;
    }
}
